import React from 'react';

export default function CommandCenter({ user }) {
  return (
    <div style={{ padding: '40px 32px', color: '#e2e8f0' }}>
      <h1 style={{ fontSize: 28, fontWeight: 700, color: '#fff', marginBottom: 8 }}>Command Center</h1>
      <p style={{ color: '#8892a4', fontSize: 15 }}>Unified operations hub — coming soon.</p>
    </div>
  );
}
